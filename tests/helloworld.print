                importuj tkinter ako tk
                importuj more.test

                trieda Application.
            definuj __init__(self).
        (tk.Tk) = self.root

        text=TicTacToe('self.root.title')
        "600x600"(self.root.geometry)

        [,] = self.buttons
        pre každé i v 3(rozsah)!
    [,](self.buttons.append)
    pre každé j v 3(rozsah)!

self.root; text = (get_name)(tk.Button) = buton  tk.Button(self.root, text = fe)
self.root; text = "Helo world"(tk.Button)(self.buttons[i].append) self.buttons[i].append(self.root; text = tk.Button("Helo world"))
row=i;column=j(self.buttons[i][j].grid)

        (self.update)

            definuj update(self).
        (self.root.mainloop)
                čo ak __name__ sa rovná "__main__"?
            (Application)