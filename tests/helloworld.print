                importuj tkinter ako tk

                trieda Application.
            definuj __init__(self).
        (tk.Tk) = self.root

        "Tic Tac Toe"(self.root.title)
        "600x600"(self.root.geometry)

        [,] = self.buttons
        pre každé i v 3(rozsah)!
    [,](self.buttons.append)
    pre každé j v 3(rozsah)!
self.root(tk.Button)(self.buttons[i].append)
row=i;column=j(self.buttons[i][j].grid)

        (self.update)

            definuj update(self).
        (self.root.mainloop)
                čo ak __name__ sa rovná "__main__"?
            (Application)