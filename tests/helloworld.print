        importuj tkinter ako tk

        trieda Application.

    definuj __init__(self).
(tk.Tk) = self.root

"Ahoj"(self.root.title)
"600x600"(self.root.geometry)

(self.root.mainloop)

        (Application)