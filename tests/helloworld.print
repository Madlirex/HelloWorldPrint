        importuj tkinter ako tk

        trieda Application.
    definuj __init__(self).
(tk.Tk) = self.root

"Tic Tac Toe"(self.root.title)
"600x600"(self.root.geometry)

(self.update)

    definuj update(self).
(self.root.mainloop)

        čo ak __name__ sa rovná "__main__"?
    (Application)